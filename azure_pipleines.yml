name: 1.0$(Rev:.r)

# simplified trigger (implied branch)
trigger: none

# equivalents trigger
# trigger:
#  branches:
#    include:
#    - main

variables:
  name: rohit

pool:
  vmImage: ubuntu-latest

jobs:

- job: helloworld
  steps:

    - script: echo "Hello,  Welcome $(name)"

stages:

- stage: Build
  jobs:

  - job: BuildJob
    steps:

    - script: echo Building!
- stage: Test
  jobs:

  - job: TestOnWindows
    steps:

    - script: echo Testing on Windows!
  - job: TestOnLinux
    steps:

    - script: echo Testing on Linux!
- stage: Deploy
  jobs:

  - job: Deploy
    steps:

    - script: echo Deploying the code!